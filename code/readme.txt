Using the py_compile Module

You can compile a single Python file by running:

python -m py_compile test_clean_data.py

This command will compile test_clean_data.py and place the resulting .pyc file in the __pycache__ directory. 

The filename will include details about the Python version (for example, test_clean_data.cpython-39.pyc for Python 3.9).